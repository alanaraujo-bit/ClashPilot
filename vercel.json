{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm turbo run build --filter=@clashpilot/web",
  "outputDirectory": "apps/web/.next",
  "regions": ["gru1"],
  "github": {
    "silent": true
  }
}
